[package]
name = "dsp"
version = "0.1.0"
edition = "2021"
authors = ["Petr Horáček <hrck@protonmail.com>"]
license = "GPL-3.0-or-later"
publish = false

[dependencies]
libm = "0.2"
defmt = { version = "0.3", optional = true }

[features]
defmt = ["dep:defmt"]
